<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   <mapper namespace="ThumbnailMapper">
   
   
  <insert id="setWrite" parameterType="ThumbnailVO">
  	insert into thumbnail values (product_seq.nextval, #{pid}, #{fname}, #{oname})
  </insert>
  
  <update id="setUpdate">
  
  </update>
  
  
  <delete id="setDelete" parameterType="java.lang.String">
  
  	delete thumbnail where pid=#{pid}
  
  </delete>
  
  <select id="getSelect">
  
  </select>
  
  
  <select id="getList">
  
  
  </select>
  
   
   
  </mapper>